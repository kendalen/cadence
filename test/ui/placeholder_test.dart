// Placeholder test for the ui layer.
//
// The ui layer has no implementation yet. This test exists so the layer is
// present in the tree and exercised by `flutter test` from the first commit; it
// will be replaced by golden tests on key screens when the first vertical slice
// lands. The ui layer is covered by golden tests only — CLAUDE.md §7.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('ui layer placeholder', () {
    expect(true, isTrue);
  });
}
