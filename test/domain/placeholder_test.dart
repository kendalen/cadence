// Placeholder test for the domain layer.
//
// The domain layer has no implementation yet. This test exists so the layer is
// present in the tree and exercised by `flutter test` from the first commit; it
// will be replaced by real tests when the first vertical slice lands
// (CLAUDE.md §7, §8). Domain logic is developed test-first — see CLAUDE.md §7.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('domain layer placeholder', () {
    expect(true, isTrue);
  });
}
