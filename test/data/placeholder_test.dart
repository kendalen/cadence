// Placeholder test for the data layer.
//
// The data layer has no implementation yet. This test exists so the layer is
// present in the tree and exercised by `flutter test` from the first commit; it
// will be replaced by real tests when the first vertical slice lands. Data code
// (serialisation, repositories, migrations) is tested thoroughly — CLAUDE.md §7.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('data layer placeholder', () {
    expect(true, isTrue);
  });
}
